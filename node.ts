const display = (a: number, b: number): number => {
  return a + b;
};

// setInterval(() => {
//   console.log(1, 2);
// }, 1000);

setTimeout(() => {
  console.log("Its time out.....");
}),
  1000;

import os from "os";
console.log(os);
